// Generated by ts-to-zod
import { z } from 'zod'

import { sNumeric } from './utils'

export const sCategory = z.object({ id: z.string().nullable(), title: z.string().nullable() })

export const sSeries = z.object({ id: sNumeric, name: z.string(), sort: z.string() })

export const sBaseComic = z.object({
  id: sNumeric.describe('\u6F2B\u753B\u7684\u552F\u4E00id'),
  name: z.string(),
  is_favorite: z.boolean(),
  liked: z.boolean(),
})

export const sLessComic = sBaseComic.extend({
  addtime: z.string(),
  images: z.array(z.string()),
  series: z.array(sSeries),
  series_id: sNumeric,
  tags: z.string(),
})

export const sCommonComic = sBaseComic.extend({
  author: z.string(),
  description: z.string().optional().nullable(),
  image: z.string(),
  category: sCategory,
  category_sub: sCategory,
  update_at: sNumeric.optional(),
})

export const sRecommendComic = z.object({
  id: sNumeric,
  author: z.string(),
  name: z.string(),
  image: z.string(),
})

export const sFullComic = sBaseComic.extend({
  images: z.array(z.string()),
  addtime: sNumeric,
  description: z.string(),
  total_views: sNumeric,
  series: z.array(sSeries),
  series_id: sNumeric,
  comment_total: sNumeric,
  author: z.array(z.string()),
  tags: z.array(z.string()),
  works: z.array(z.string()),
  actors: z.array(z.string()),
  related_list: z.array(sRecommendComic),
  liked: z.boolean(),
  is_aids: z.boolean(),
  price: z.union([sNumeric, z.literal('')]),
  purchased: z.union([sNumeric, z.literal('')]),
  likes: sNumeric,
})